repos:

-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.13.2
    hooks:
    # Run the linter.
    -   id: ruff-check
        # args: [--statistics]
        args: [--fix, --show-fixes, --unsafe-fixes]
    # Run the formatter.
    -   id: ruff-format
        # args: [--diff]
